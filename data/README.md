# Data

Dataset files live here.

- `raw/`: original images and videos
- `processed/`: cleaned images and labels
- `yolo/`: YOLO train/val/test folders and `dataset.yaml`
- `samples/`: small demo or test images
- `calibration/`: camera-to-robot calibration matrices used at runtime

Do not commit large datasets unless they are intentionally small samples.

