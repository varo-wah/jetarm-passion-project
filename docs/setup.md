# Setup

Install Python dependencies:

```bash
python3 -m pip install -r requirements.txt
```

ROS 2 dependencies such as `rclpy`, `sensor_msgs`, `cv_bridge`, and `ros_robot_controller_msgs` must be installed through the JetArm/ROS environment.

