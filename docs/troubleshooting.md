# Troubleshooting

- If imports fail, install the project package or run commands from the repository root with `PYTHONPATH=src`.
- If ROS imports fail, verify the JetArm ROS 2 environment is sourced.
- If pixel-to-robot conversion fails, verify the calibration files exist in `data/calibration/`.

